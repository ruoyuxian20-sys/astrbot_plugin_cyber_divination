[lint]
# main.py 需要防御式捕获 AstrBot 各版本事件 API 的差异（方法/属性可能不存在），
# 这些盲捕获与静默降级是有意为之：任何失败都让功能优雅回退，不中断插件。
# 干支/节气按本地天真时间（naive datetime）计算，DTZ 类规则不适用。
ignore = ["BLE001", "S110", "S112", "DTZ001", "DTZ005", "DTZ007", "DTZ011"]
